// Function to find the number of subarrays whose sum equals k\nfunction subarraySum(nums: number[], k: number): number {\n    const countMap = new Map<number, number>(); // Map to store prefix sums and their counts\n    let count = 0;\n    let currentSum = 0;\n\n    countMap.set(0, 1); // Initialize with sum 0 count as 1\n\n    for (let num of nums) {\n        currentSum += num;\n        if (countMap.has(currentSum - k)) {\n            count += countMap.get(currentSum - k)!; // Add count of previous prefix sums that contribute to k\n        }\n        countMap.set(currentSum, (countMap.get(currentSum) || 0) + 1); // Increment prefix sum count\n    }\n    return count;\n}\n\n// Time complexity: O(n)\n// Space complexity: O(n)\n\nexport default subarraySum;
