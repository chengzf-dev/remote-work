import subarraySum from './solution';\n\ntest('Subarray Sum Equals K Test Cases', () => {\n    expect(subarraySum([1, 1, 1], 2)).toEqual(2); // Two subarrays [1,1] at index 0-1 and 1-2\n    expect(subarraySum([1, 2, 3], 3)).toEqual(2); // Subarrays: [1,2] and [3]\n    expect(subarraySum([0, 0, 0], 0)).toEqual(6); // All subarrays sum to 0\n    expect(subarraySum([-1, -1, 1], 0)).toEqual(1); // Single subarray: [-1, -1, 1]\n    expect(subarraySum([2, -1, 2, -1], 2)).toEqual(4); // Subarrays: [2], [-1, 2, -1], [2, -1], and [2,-1]\n});
