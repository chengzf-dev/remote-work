import assert from 'assert';\nimport subarraySum from './solution';\n\ndescribe('Subarray Sum Equals K Tests', () => {\n    it('should count subarrays with sum equal to k', () => {\n        assert.strictEqual(subarraySum([1, 1, 1], 2), 2); // Two subarrays: [1,1] and [1,1]\n        assert.strictEqual(subarraySum([1, 2, 3], 3), 2); // [3] and [1,2]\n        assert.strictEqual(subarraySum([0, 0, 0], 0), 6); // Six subarrays contributing to 0\n        assert.strictEqual(subarraySum([-1, -1, 1], 0), 1); // One subarray: [-1, -1, 1]\n    });\n});
