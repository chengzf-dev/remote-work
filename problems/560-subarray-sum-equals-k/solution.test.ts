import { strict as assert } from 'assert';\nimport { subarraySum } from './solution';\n\ntest('Subarray Sum Equals K Test Cases', () => {\n    // Test Case 1: Basic case\n    assert.equal(subarraySum([1, 1, 1], 2), 2);\n    // Test Case 2: One negative number\n    assert.equal(subarraySum([-1, -1, 1, 1], 0), 4);\n    // Test Case 3: Edge case with zero k\n    assert.equal(subarraySum([0, 0, 0], 0), 6);\n    // Test Case 4: Mixed cases\n    assert.equal(subarraySum([2, -1, 2, -1], 2), 3);\n});
