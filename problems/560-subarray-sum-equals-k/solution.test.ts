import assert from 'assert';\nimport subarraySum from './solution';\n\n// Test cases for Subarray Sum Equals K function\ndescribe('subarraySum', () => {\n    it('should return 2 for input [1, 1, 1] and k = 2', () => {\n        assert.strictEqual(subarraySum([1, 1, 1], 2), 2);\n    });\n    it('should return 0 for input [1, 2, 3] and k = 7', () => {\n        assert.strictEqual(subarraySum([1, 2, 3], 7), 0);\n    });\n    it('should return 3 for input [2, -1, 2, 1, -1, 2] and k = 2', () => {\n        assert.strictEqual(subarraySum([2, -1, 2, 1, -1, 2], 2), 3);\n    });\n    it('should return 1 for input [-1, -1, 1] and k = 0', () => {\n        assert.strictEqual(subarraySum([-1, -1, 1], 0), 1);\n    });\n    it('should return 0 for input [] and k = 0', () => {\n        assert.strictEqual(subarraySum([], 0), 0);\n    });\n});
