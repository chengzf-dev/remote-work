import twoSum from './solution';\n\ntest('Two Sum Test Cases', () => {\n    expect(twoSum([2, 7, 11, 15], 9)).toEqual([0, 1]); // Target sum is found\n    expect(twoSum([3, 2, 4], 6)).toEqual([1, 2]); // Target sum is found\n    expect(twoSum([3, 3], 6)).toEqual([0, 1]); // Duplicates handled\n    expect(twoSum([1, 2, 3, 4], 8)).toEqual([]); // No solution\n    expect(twoSum([-3, 4, 3, 90], 0)).toEqual([0, 2]); // Negative values\n});
