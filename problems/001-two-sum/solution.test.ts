import assert from 'assert';\nimport twoSum from './solution';\n\ndescribe('Two Sum Tests', () => {\n    it('should return indices of numbers that add up to target', () => {\n        assert.deepStrictEqual(twoSum([2, 7, 11, 15], 9), [0, 1]); // 2 + 7 = 9\n        assert.deepStrictEqual(twoSum([3, 2, 4], 6), [1, 2]); // 2 + 4 = 6\n        assert.deepStrictEqual(twoSum([3, 3], 6), [0, 1]); // 3 + 3 = 6\n        assert.throws(() => twoSum([1, 2, 3], 7), /No two sum solution/); // No solution\n    });\n});
