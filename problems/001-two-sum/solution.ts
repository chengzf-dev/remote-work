// Function to find indices of two numbers that add up to target\nfunction twoSum(nums: number[], target: number): number[] {\n    const indexMap = new Map<number, number>(); // to store number and its index\n    for (let i = 0; i < nums.length; i++) {\n        const complement = target - nums[i];\n        if (indexMap.has(complement)) {\n            return [indexMap.get(complement)!, i]; // Return indices\n        }\n        indexMap.set(nums[i], i); // Store index of the number\n    }\n    throw new Error('No two sum solution'); // Handle case where no solution\n}\n\n// Time complexity: O(n)\n// Space complexity: O(n)\n\nexport default twoSum;
