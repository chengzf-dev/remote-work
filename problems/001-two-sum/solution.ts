// Function to return indices of the two numbers that add up to target\nfunction twoSum(nums: number[], target: number): number[] {\n    const map = new Map<number, number>();\n    for (let i = 0; i < nums.length; i++) {\n        const complement = target - nums[i];\n        if (map.has(complement)) {\n            return [map.get(complement)!, i];\n        }\n        map.set(nums[i], i);\n    }\n    return []; // return empty array if no solution\n}\n\n// Time Complexity: O(n) - as we traverse the list only once\n// Space Complexity: O(n) - we use a hash map to store elements\n\nexport default twoSum;\n
