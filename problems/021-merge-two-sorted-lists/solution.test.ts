import { mergeTwoLists, ListNode } from './solution';\n\ntest('Merge Two Sorted Lists Test Cases', () => {\n    expect(mergeTwoLists(null, null)).toBeNull(); // Both lists empty\n    expect(mergeTwoLists(new ListNode(1), null)).toEqual(new ListNode(1)); // One list empty\n    const l1 = new ListNode(1, new ListNode(3));\n    const l2 = new ListNode(2, new ListNode(4));\n    const merged = mergeTwoLists(l1, l2);\n    expect(merged).toEqual(new ListNode(1, new ListNode(2, new ListNode(3, new ListNode(4))))); // Both lists merged\n    const l3 = new ListNode(1, new ListNode(2, new ListNode(3)));\n    const l4 = new ListNode(4, new ListNode(5));\n    const merged2 = mergeTwoLists(l3, l4);\n    expect(merged2).toEqual(new ListNode(1, new ListNode(2, new ListNode(3, new ListNode(4, new ListNode(5)))))); // Merging longer lists\n});
