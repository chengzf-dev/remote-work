import { strict as assert } from 'assert';\nimport { mergeTwoLists, ListNode } from './solution';\n\ntest('Merge Two Sorted Lists Test Cases', () => {\n    // Test Case 1: Both lists not empty\n    const l1 = new ListNode(1);\n    l1.next = new ListNode(2);\n    const l2 = new ListNode(1);\n    l2.next = new ListNode(3);\n    const merged = mergeTwoLists(l1, l2);\n    assert.deepEqual(merged.value, 1); // first value\n    assert.deepEqual(merged.next.value, 1); // second value\n    assert.deepEqual(merged.next.next.value, 2); // third value\n    assert.deepEqual(merged.next.next.next.value, 3); // fourth value\n\n    // Test Case 2: One list empty\n    const l3 = mergeTwoLists(null, l2);\n    assert.deepEqual(l3.value, 1); // first value\n\n    // Test Case 3: Both lists empty\n    const l4 = mergeTwoLists(null, null);\n    assert.deepEqual(l4, null);\n\n    // Test Case 4: Equal lengths\n    const l5 = new ListNode(1);\n    l5.next = new ListNode(4);\n    const l6 = new ListNode(2);\n    l6.next = new ListNode(3);\n    const mergedEqual = mergeTwoLists(l5, l6);\n    assert.deepEqual(mergedEqual.value, 1); // first value\n    assert.deepEqual(mergedEqual.next.value, 2); // second value\n});
