import assert from 'assert';\nimport { ListNode, mergeTwoLists } from './solution';\n\n// Test cases for Merge Two Sorted Lists function\ndescribe('mergeTwoLists', () => {\n    it('should merge two sorted lists [1 -> 2 -> 4] and [1 -> 3 -> 4]', () => {\n        const l1 = new ListNode(1);\n        l1.next = new ListNode(2);\n        l1.next.next = new ListNode(4);\n\n        const l2 = new ListNode(1);\n        l2.next = new ListNode(3);\n        l2.next.next = new ListNode(4);\n\n        const merged = mergeTwoLists(l1, l2);\n        const values = [];\n        while (merged) {\n            values.push(merged.value);\n            merged = merged.next;\n        }\n        assert.deepEqual(values, [1, 1, 2, 3, 4, 4]);\n    });\n\n    it('should return l2 when l1 is null', () => {\n        const l2 = new ListNode(0);\n        assert.deepEqual(mergeTwoLists(null, l2), l2);\n    });\n\n    it('should return l1 when l2 is null', () => {\n        const l1 = new ListNode(1);\n        assert.deepEqual(mergeTwoLists(l1, null), l1);\n    });\n\n    it('should handle both lists empty', () => {\n        assert.deepEqual(mergeTwoLists(null, null), null);\n    });\n});
