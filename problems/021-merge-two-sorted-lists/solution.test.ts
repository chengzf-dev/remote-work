import assert from 'assert';\nimport { mergeTwoLists, ListNode } from './solution';\n\ndescribe('Merge Two Sorted Lists Tests', () => {\n    it('should merge two sorted linked lists', () => {\n        const l1 = new ListNode(1, new ListNode(2, new ListNode(4)));\n        const l2 = new ListNode(1, new ListNode(3, new ListNode(4)));\n        const result = mergeTwoLists(l1, l2);\n        assert.deepStrictEqual(result, new ListNode(1, new ListNode(1, new ListNode(2, new ListNode(3, new ListNode(4, new ListNode(4)))))));\n    });\n    it('should handle empty lists', () => {\n        assert.deepStrictEqual(mergeTwoLists(null, null), null);\n        const l1 = new ListNode(0);\n        assert.deepStrictEqual(mergeTwoLists(l1, null), l1);\n    });\n    it('should handle one empty list', () => {\n        const l2 = new ListNode(1);\n        assert.deepStrictEqual(mergeTwoLists(null, l2), l2);\n    });\n});
